import styled from 'styled-components';

//! Deleted after first version commit

const Centered = styled.div`
   width: 100%;
   display: flex;
   justify-content: center;
   align-items: center;
   font-weight: bold;
   font-size: 3rem;
`

export default Centered;